describe('Térkép E2E', () => {
  it('Lehet pontot kijelölni a térképen', () => {
    cy.visit('/');
    // cy.get('[data-testid="map-container"]').click(100, 100);
    // cy.get('[data-testid="selected-point"]').should('exist');
  });
});
